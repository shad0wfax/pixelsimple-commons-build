Commons Build Repository
-------------------------------
This repo contains the master build project. 
Since we are using Ant + Ivy, our builds will be command line oriented. This project contains the common build files that can be used by other project repos. 

This repo needs to be cloned first before individual repos can be built. The other repos's/module's depend on the build files from this repo.

The intent of this project is to abstract the build process and harmonize the build process across different repos/modules/projects. Other repos refer to this.
